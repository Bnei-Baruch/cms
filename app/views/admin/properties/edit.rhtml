<% @title = "Edit property" %>

<%= render :partial => 'form',
    :locals => {
    :path => admin_property_path(@property),
    :method => :put,
    :button_text => 'Update'
  } %>

<%= link_to 'Cancel', admin_properties_path %>