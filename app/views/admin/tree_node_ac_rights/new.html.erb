<% @title = "New tree node access right" %>

<%= render :partial => 'form',
    :locals => {
    :path => admin_tree_node_ac_rights_path,
    :method => :post,
    :button_text => 'Create'
  } %>

<%= link_to 'Back', admin_tree_node_ac_rights_path %>
