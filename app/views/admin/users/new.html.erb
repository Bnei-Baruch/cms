<% @title = "New user" %>

<%= render :partial => 'form',
    :locals => {
    :path => admin_users_path,
    :method => :post,
    :button_text => 'Create'
  } %>

<%= link_to 'Back', admin_users_path %>
