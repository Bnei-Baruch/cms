<h1>New Label</h1>
<%= error_messages_for :label %>

<%= render :partial => "#{@label.class.to_s.underscore}_form",
  :locals => {
    :path => item_label_path(@item,@label),
    :method => :put,
    :button_text => 'Update'
  } %>

<%= link_to 'Cancel', item_labels_path(@item) %>