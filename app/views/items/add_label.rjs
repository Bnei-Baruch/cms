page.insert_html :bottom, :labels, :partial => 'label_fields', :locals => { :label => @label, :index => params[:index] }
page.replace :add_label_link, :partial => 'add_label_link', :locals => { :index => (params[:index].to_i + 1) }
