<% for column in Label.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @label.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @label %> |
<%= link_to 'Back', :action => 'list' %>
